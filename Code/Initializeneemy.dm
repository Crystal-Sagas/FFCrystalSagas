/**
 * InitializeEnemy - Legacy enemy combat initialization (STUBBED)
 *
 * NOTE: Combat system is being replaced. This proc is kept as a stub
 * to prevent compile errors from existing calls in Monster.*.dm files.
 *
 * When the new combat system is implemented, update the callers
 * (Monster.ARank.dm, Monster.BRank.dm, etc.) to use the new system.
 */
/proc/InitializeEnemy(mob/npc/enemy)
	return // Stub - combat system being rebuilt